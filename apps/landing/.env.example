# Build-time environment variables for the landing (Vite `import.meta.env`).
# Copy to .env.local and fill in as features that need them land.

# Web3Forms access key for the Contact / Report-a-bug forms. Until set, the
# forms run in DEMO mode (simulated success — no request is sent).
# VITE_WEB3FORMS_KEY=
