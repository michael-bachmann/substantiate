// The background/PDF path uses the `chrome.*` global (debugger + downloads).
// TypeScript 6.0 stopped auto-including @types/chrome's ambient `chrome`
// namespace, so pull it in explicitly here.
/// <reference types="chrome" />
